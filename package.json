{
  "name": "for-chelsea",
  "private": true,
  "version": "1.0.0",
  "description": "An interactive birthday card for Chelsea.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/inline.mjs && node scripts/artifact.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
